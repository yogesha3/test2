<?xml version="1.0" encoding="utf-8"?>

<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">

        <shape>
            <corners android:radius="10dip" />
            <gradient
                android:startColor="@color/progress_bar_color"
                android:centerColor="@color/progress_bar_color"
                android:centerY="1.0"
                android:endColor="@color/progress_bar_color"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="10dip" />
                <gradient
                    android:startColor="@color/bottom_tab_text_color_selected"
                    android:centerColor="@color/bottom_tab_text_color_selected"
                    android:centerY="1.0"
                    android:endColor="@color/progress_bar_color"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

</layer-list>